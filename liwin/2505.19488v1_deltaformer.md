# Hiểu Transformer qua Lăng kính Bộ nhớ Liên kết
(5/6/2025)

###### Tóm tắt

Bài viết phân tích kiến trúc Transformer từ góc độ bộ nhớ liên kết - khái niệm tâm lý mô phỏng cách não người ghi nhớ. Chúng tôi tập trung vào 2 khía cạnh:

Dung lượng bộ nhớ: Đo lường bằng SNR truy xuất, giải thích tại sao Softmax Attention hiệu quả hơn Linear Attention qua phân tích kernel. FFN cũng được xem như bộ nhớ liên kết dùng kernel ReLU.

Cập nhật bộ nhớ: Phân tích cơ chế cập nhật tri thức trong các biến thể Transformer (DeltaNet, Softmax Attention). Đặt 2 câu hỏi then chốt: 1. Transformer có giới hạn biểu diễn không? 2. Ngữ cảnh vô hạn có tạo ra trí tuệ vô hạn?

## 1 Giới thiệu

Transformer có thể được hiểu qua lăng kính bộ nhớ liên kết - khả năng não bộ ghi nhớ quan hệ giữa các thực thể. Cụ thể:

- Attention: Bộ nhớ ngắn hạn, liên kết ngữ cảnh
- FFN: Bộ nhớ dài hạn, mã hóa tri thức huấn luyện

Chúng tôi phân tích qua 2 chiều:

`Dung lượng bộ nhớ`: Giới thiệu SNR truy xuất, chứng minh Softmax Attention vượt trội nhờ kernel mũ. FFN dùng kernel ReLU cũng có thể xem như bộ nhớ liên kết.

`Cập nhật bộ nhớ`: Đề xuất framework thống nhất cho các cơ chế cập nhật. DeltaNet dùng delta-rule cho khả năng biểu diễn tốt hơn Softmax Attention. Chúng tôi kết hợp ưu điểm cả hai trong mô hình mới.

Hai câu hỏi đáng quan tâm:

1. Giới hạn biểu diễn của Transformer có thể vượt qua?
2. Ngữ cảnh vô hạn có thực sự giúp mô hình thông minh vô hạn? Phân tích cho thấy bộ nhớ có thể hội tụ khi ngữ cảnh tăng, gây khó khăn cho học ngữ cảnh.

## 2 Bộ nhớ Liên kết

_Bộ nhớ liên kết là gì?_ Theo Wikipedia, đây là khả năng ghi nhớ mối quan hệ giữa các thực thể không trực tiếp liên quan. Ví dụ: người từng thấy tháp Eiffel ở Paris sẽ liên tưởng hai hình ảnh này. Khi nghe "Paris", não tự động gợi nhớ "tháp Eiffel". Về mặt học thuật, Paris đóng vai trò _khóa_ kích hoạt _giá trị_ tương ứng (tháp Eiffel) trong bộ nhớ. Cơ chế này nền tảng cho nhận thức, giúp truy xuất thông tin liên quan hiệu quả.

![](https://pbs.twimg.com/media/GwMJpAnXcAMzNnP?format=jpg)

**Hình thức hóa bộ nhớ liên tưởng:** Định nghĩa ma trận **S_t** ∈ ℝ^(d_v×d_k) biểu diễn bộ nhớ tại thời điểm t, lưu các cặp (k, v) với k ∈ ℝ^(d_k), v ∈ ℝ^(d_v). Cập nhật bộ nhớ bằng tích lũy tích ngoài:

$$S_t = \sum_{i=1}^{t} v_i k_i^T$$

**Truy xuất giá trị:** Định nghĩa ánh xạ f_S(q) = Sq để nhớ lại giá trị từ khóa q ≈ k_i. Ánh xạ này có khả năng chịu lỗi [15,19].

**Transformer như bộ nhớ liên tưởng:** Lớp attention có thể viết dưới dạng kernel:

**Transformer thực sự có thể được diễn giải như bộ nhớ liên tưởng.** Lấy lớp attention, một thành phần cốt lõi của Transformer, làm ví dụ, đầu ra o_t của nó tại thời điểm t có thể được tính như sau:

Định nghĩa ánh xạ đặc trưng tương ứng với hàm nhân exp là φ(·), sao cho exp(k^T q/√d_k) = φ(k)^T φ(q). Để đơn giản, chúng ta bỏ qua số hạng chuẩn hóa. Phép tính o_t có thể được viết lại như sau:

$$o_t = \sum_{i=1}^{t} \phi(k_i)^T \phi(q_t) v_i$$

$$= \sum_{i=1}^{t} v_i \phi(k_i)^T \phi(q_t)$$

$$= \underbrace{\sum_{i=1}^{t} v_i \phi(k_i)^T}_{S_t} \phi(q_t)$$
  
Ta thấy lớp attention là một dạng bộ nhớ liên kết, với:
$$S_t = \sum_{i=1}^t v_i \phi(k_i)^T$$
$$f_S(q) = S \phi(q)$$

FFN cũng có thể xem như bộ nhớ liên kết (xem mục 2.1.1 về kernel ReLU). 

Dù cả attention và FFN đều là bộ nhớ liên kết, chúng khác nhau về thời gian lưu trữ:
- `Attention`: Bộ nhớ ngắn hạn (KV cache), bị xóa sau khi suy luận
- `FFN`: Bộ nhớ dài hạn, nén qua gradient descent khi huấn luyện và giữ nguyên sau đó

Có thể phân tích Transformer qua 2 khía cạnh:
1. Dung lượng bộ nhớ (mục 2.1)
2. Cập nhật bộ nhớ (mục 2.2)

### 2.1 Dung lượng bộ nhớ

Cụ thể, xét $q_t = k_i$, việc truy xuất bất kỳ $v_i$ nào được thực hiện như sau:

$$o_t = S_t k_i = \underbrace{v_i(k_i^T k_i)}_{\text{số hạng tín hiệu}} + \underbrace{\sum_{j \neq i} v_j(k_j^T k_i)}_{\text{số hạng nhiễu}}.$$

Để đạt được việc truy xuất chính xác, yêu cầu các vector $\{k_1, ..., k_t\}$ phải trực giao, ngụ ý rằng trong Phương trình 4, $k_i^T k_i = 1$ và $k_j^T k_i = 0$ với $j \neq i$.

**Dung lượng của bộ nhớ liên tưởng $S_t$ là bao nhiêu?** Tức là, trong điều kiện nhớ lại chính xác, số lượng cặp khóa-giá trị tối đa mà nó có thể lưu trữ là bao nhiêu? Trực giác cho thấy, không gian tuyến tính được kéo dài bởi các vector $k_i$ chỉ có $d_k$ cơ sở trực giao. Nếu chúng ta lưu trữ các cặp khóa-giá trị $n >> d_k$, "số hạng nhiễu" $\sum_{j \neq i} v_j(k_j^T k_i)$ trong Phương trình 4 trở nên khác không, dẫn đến lỗi truy xuất tăng lên.

Để phân tích định lượng hiệu suất của việc truy xuất bộ nhớ liên tưởng mờ, chúng tôi định nghĩa một chỉ số tương tự như Tỷ lệ Tín hiệu trên Nhiễu (SNR). Cụ thể, xét phép toán truy xuất tổng quát:

$$S_t k_i = cv_i + r,$$

trong đó số hạng đầu tiên biểu diễn tín hiệu, được nhân với hệ số $c$, và số hạng thứ hai biểu diễn nhiễu $r$. SNR nghịch đảo được định nghĩa như sau:

$$\text{SNR}^{-1} = \mathbb{E}_{v_j, k_j} \left[ \frac{\|r\|^2}{c^2 \|v_i\|^2} \right].$$

Chỉ số này đo lường trực quan tỷ lệ nhiễu so với tín hiệu. Giá trị lớn hơn cho thấy thành phần nhiễu cao hơn, dẫn đến độ chính xác truy xuất thấp hơn. Chúng tôi xét kịch bản trong đó các vector khóa $k_j \in \mathbb{R}^{d_k}$ và vector giá trị $v_j \in \mathbb{R}^{d_v}$ là các vector Gaussian chuẩn phân phối độc lập và đồng nhất (i.i.d). Dưới giả định này, chúng ta có thể tính toán định lượng SNR nghịch đảo, cung cấp một thước đo rõ ràng về sự suy giảm độ chính xác truy xuất.

Đối với dạng tuyến tính trong Phương trình 4, số hạng tín hiệu là $c = k_i^T k_i$, và nhiễu được cho bởi $r = \sum_{j \neq i} v_j(k_j^T k_i)$. Như được chỉ ra trong phụ lục A, điều này dẫn đến biểu thức xấp xỉ sau:

$$\boxed{\text{SNR}_{\text{Linear}}^{-1} \approx \frac{N}{d_k}}$$

Kết quả này cho thấy:
- Nhiễu tăng tuyến tính với N
- Cần dₖ ≳ N để đảm bảo truy xuất chính xác
- Giải thích tại sao Linear Attention kém hiệu quả với ngữ cảnh dài

Tiếp theo sẽ trình bày hai phương pháp cải thiện: kernel mapping và sparsity.

### 2.1.1 Ánh xạ Kernel (Kernel Mapping)

Vì dung lượng bộ nhớ bị giới hạn bởi $d_k$, chúng ta có thể áp dụng hàm kernel để ánh xạ các vector lên không gian chiều cao hơn, cho phép việc khớp truy vấn-khóa xảy ra trong không gian mở rộng này. Một cách trực quan, trong không gian chiều cao hơn, các vector trở nên dễ phân biệt với nhau hơn, khiến chúng dễ tách biệt hơn, giảm lỗi truy xuất, và cho phép bộ nhớ lưu trữ nhiều giá trị $v$ hơn. Hiểu biết này có liên quan chặt chẽ đến bổ đề Johnson-Lindenstrauss và lý thuyết chồng chập [9, 14].

Bằng cách giới thiệu hàm kernel $\kappa(x, y) = \phi(x)^T\phi(y)$, quá trình ánh xạ trở thành $f_{S_t}(k_t) = S_t\phi(k_t)$. Tương ứng, bộ nhớ liên tưởng và thao tác nhớ lại là:

$$S_t = \sum_{i=1}^{t} v_i\phi(k_i)^T,$$

$$o_t = S_t\phi(q_t) = \sum_{i=1}^{t} v_i(\phi(k_i)^T\phi(q_t)) = \sum_{i=1}^{t} v_i\kappa(k_i, q_t).$$

Bây giờ chúng tôi giới thiệu phân tích SNR trong thiết lập kernel hóa. Theo phép tính từ phụ lục A, chúng ta có:

$$\text{SNR}_\kappa^{-1} = N\frac{\mathbb{E}_{k_j}[\kappa^2(k_j, k_i)]}{\kappa^2(k_i, k_i)}$$

Trong trường hợp đặc biệt khi hàm kernel là tuyến tính, tức là $\kappa(x, y) = x^T y$, công thức kernel hóa khôi phục lại SNR nghịch đảo trường hợp tuyến tính ban đầu trong Phương trình 7. Tiếp theo, chúng tôi phân tích hai hàm kernel đặc biệt quan trọng trong bộ nhớ liên tưởng: kernel mũ (Exp) và kernel ReLU, do mối liên hệ chặt chẽ của chúng với cơ chế attention và mạng truyền thẳng (FFN) thường được sử dụng trong các kiến trúc transformer hiện đại.

⁴Lưu ý rằng số hạng chuẩn hóa được bỏ qua vì nó không ảnh hưởng đến việc tính toán SNR nghịch đảo.

**Exp Kernel.** Chúng tôi định nghĩa kernel mũ là $\kappa_{\exp}(x, y) = \exp(\frac{x^T y}{\tau})$, trong đó $\tau > 1$ là tham số nhiệt độ. Thông thường, $\tau$ được đặt là $\sqrt{d_k}$ để đảm bảo tỷ lệ phù hợp, vì tích vô hướng $x^T y$ có phương sai $d_k$ (giả sử các thành phần độc lập với trung bình bằng không và phương sai đơn vị). Với lựa chọn này, công thức kernel tương ứng trực tiếp với cơ chế Softmax Attention chuẩn⁴: $\sum_{i=1}^t v_i \exp(k_i, q_t)$. Thực tế, kernel mũ cho phép ánh xạ đặc trưng rõ ràng vào không gian Hilbert vô hạn chiều, có thể được biểu diễn dưới dạng một chuỗi lũy thừa vô hạn⁵:

$$\phi(\cdot) = \begin{bmatrix} 1 \\ (\cdot) \\ \frac{(\cdot)^{\otimes 2}}{\sqrt{2!}} \\ \vdots \\ \frac{(\cdot)^{\otimes \infty}}{\sqrt{\infty!}} \end{bmatrix}, \quad (11)$$

trong đó $(\cdot)^{\otimes m}$ biểu thị tích Kronecker m lần của véc-tơ với chính nó. Chúng tôi bỏ qua $\tau$ ở đây để rõ ràng. Cụ thể, ta có:

$$\mathbf{S}_t\phi(\mathbf{q}_t) = \sum_{i=1}^{t} \left(\phi(\mathbf{q}_j)^\top\phi(\mathbf{k}_i)\right) \mathbf{v}_i = \sum_{i=1}^{t} \sum_{m=0}^{\infty} \frac{(\mathbf{q}_t^{\otimes m})^\top(\mathbf{k}_i^{\otimes m})}{m!} \mathbf{v}_i = \sum_{i=1}^{t} \sum_{m=0}^{\infty} \frac{(\mathbf{q}_t^\top \mathbf{k}_i)^m}{m!} \mathbf{v}_i = \sum_{i=1}^{t} \exp(\mathbf{q}_t^\top \mathbf{k}_i)\mathbf{v}_i. \quad (12)$$

Dưới cùng giả định rằng các khóa và giá trị là các véc-tơ Gauss chuẩn độc lập đồng phân phối (i.i.d.), như được suy ra trong phụ lục A, SNR nghịch đảo của hạt nhân exp có thể được xấp xỉ như sau:

$$\boxed{\text{SNR}_{\text{exp}}^{-1} \approx \frac{N}{\exp\left(\frac{2(\tau-1)}{\tau^2}d_k\right)}.} \quad (13)$$

Kết quả này cho thấy hạt nhân hàm mũ làm giảm đáng kể số chiều đặc trưng $d_k$ cần thiết cho việc truy xuất chính xác: từ $\mathcal{O}(N)$ trong trường hợp tuyến tính (Phương trình 7) xuống còn $\mathcal{O}(\log^2 N)$ khi $\tau = \sqrt{d_k}$ với hạt nhân hàm mũ.

Phát hiện lý thuyết này có thể được hỗ trợ bởi một số quan sát thực nghiệm:

1. **Mở rộng số chiều trong LLMs**: Trong huấn luyện LLM thực tế với Softmax Attention, việc tăng số chiều $d$ trở nên cần thiết khi độ dài ngữ cảnh $N$ tăng để duy trì độ chính xác truy xuất, phù hợp trực tiếp với mối quan hệ SNR mà chúng tôi đã suy ra [46].

2. **Hiệu ứng nhiệt độ**: Giảm tham số nhiệt độ $\tau$ - trong khi vẫn giữ nó lớn hơn 1 - cải thiện độ chính xác truy xuất, phù hợp với các phát hiện của [27, 44].

3. **Đánh đổi hiệu quả đa đầu**: Chúng tôi giả thuyết rằng khả năng truy xuất vượt trội của Softmax Attention cho phép nó đánh đổi số chiều để tăng khả năng biểu diễn thông qua các kiến trúc đa đầu, trong khi Linear Attention hưởng lợi nhiều hơn từ số chiều cao hơn. Thí nghiệm sơ bộ của chúng tôi trong Mục 3.1 hỗ trợ kết luận này.


**Hạt nhân ReLU.** Chúng tôi chỉ ra rằng mạng truyền thẳng chuẩn (FFN), bao gồm hai lớp tuyến tính không có bias và một hàm kích hoạt phi tuyến ReLU có thể được diễn giải như một bộ nhớ liên kết được định nghĩa bởi một hạt nhân ReLU⁶. Xét FFN được cho bởi FFN(**x**) = **W**_V^⊤ ReLU(**W**_K **x**), trong đó **x** ∈ ℝ^d là đầu vào, **W**_K, **W**_V ∈ ℝ^(m×d) là các ma trận trọng số, và hàm ReLU được áp dụng theo từng phần tử: ReLU(z) = max(0, z). Với công thức trên, chúng ta có thể ánh xạ trực tiếp FFN thành một bộ nhớ liên kết:

FFN(**x**) = **W**_V^⊤ ReLU(**W**_K **x**)

= ∑_{i=1}^m **W**_{V_i}^⊤ ReLU(**W**_{K_i} **x**).                    (14)

Trong đó:
- **v**_i = **W**_{V_i}^⊤ (giá trị)
- κ(·,·) = ReLU(·) (hàm hạt nhân)  
- **k**_i = **W**_{K_i} (khóa)
- **q**_t = **x** (truy vấn)

---

⁵Trên thực tế, do độ chính xác hạn chế, các thí nghiệm của chúng tôi phát hiện ra rằng khai triển này thực tế bị cắt ngắn ở khoảng bậc 7 đến bậc 8, thay vì tiếp tục vô hạn.

⁶Các loại hàm kích hoạt FFN khác có thể được phân tích bằng các phương pháp tương tự; ở đây chúng tôi chọn những hàm đại diện nhất. Ngoài ra, chúng tôi không phân tích hàm kích hoạt SiLU riêng biệt, vì hành vi của nó tương tự như ReLU và không ảnh hưởng đến kết luận của chúng tôi.

---

điều này phù hợp với Phương trình 9, trong đó các hàng trong hai lớp tuyến tính **W**_{U_i}, **W**_{D_i} định nghĩa m khóa bộ nhớ **k**_i và giá trị **v**_i, và hàm hạt nhân κ_{ReLU}(**x**, **y**) = ReLU(**x**^⊤**y**). Từ góc nhìn này, chúng ta có thể quan sát thấy rằng các cơ chế bộ nhớ cơ bản của FFN và Attention về cơ bản là thống nhất. Sự khác biệt chính là Attention duy trì các bộ nhớ liên kết phụ thuộc ngữ cảnh (ví dụ: họ và tên của một người mới trong ngữ cảnh), trong khi FFN mã hóa các bộ nhớ bền vững phản ánh kiến thức thế giới tổng quát hơn (ví dụ: tên của một người nổi tiếng). Sự tương đồng về cấu trúc này gợi ý rằng thiết kế của chúng có thể lấy cảm hứng lẫn nhau và điều này đã được công nhận [42]. Chúng tôi sẽ thảo luận về các khả năng tích hợp này trong phần tiếp theo.

Theo cách suy dẫn được trình bày trong phụ lục A, chúng tôi xấp xỉ SNR nghịch đảo cho hạt nhân ReLU như sau:

$$\boxed{\text{SNR}_{\text{ReLU}}^{-1} \approx \frac{N}{2d_k}.} \quad (15)$$

So với trường hợp tuyến tính (Phương trình 7), hạt nhân ReLU cải thiện hiệu suất truy xuất bằng cách triệt tiêu các cặp truy vấn-khóa có độ tương đồng âm về không. Tuy nhiên, độ chính xác của nó vẫn kém đáng kể so với hạt nhân hàm mũ được sử dụng trong các cơ chế Attention. Chúng tôi sẽ thảo luận điều này tiếp theo.

### 2.1.2 Góc nhìn 1: Suy nghĩ lại về Attention và FFN từ góc độ Bộ nhớ Liên kết

**Bảng 1** So sánh các thành phần kiến trúc điển hình giữa Attention và FFN.

| Thành phần | Attention | FFN |
|------------|-----------|-----|
| Hạt nhân | Exp | ReLU / SiLU |
| Chuẩn hóa | Có | Không |
| Đa đầu | Có | Không |
| Thưa | Không | MoE |
| Cổng | Không | SwiGLU Gating |

Sự đối xứng giữa Attention và FFN trong Transformers rất thú vị, bởi vì—ít nhất về nguyên tắc—bất kỳ thiết kế nào cho một module cũng có thể được thực hiện hoàn toàn giống cho module kia. Sự đối xứng này khiến chúng ta phải xem xét sâu hơn về sự khác biệt của chúng và đồng thời, truyền cảm hứng cho chúng ta khám phá các thiết kế kiến trúc mới.

Bảng 1 tóm tắt một số thiết kế cổ điển phổ biến nhất được chia sẻ bởi hai module. Trong phần tiếp theo, chúng tôi tập trung vào sự khác biệt của chúng ở hạt nhân, sau đó sẽ đề cập ngắn gọn về chuẩn hóa, đa đầu, thưa và cổng. Việc khám phá sâu hơn về các chủ đề này sẽ được để lại cho các nghiên cứu trong tương lai; chúng tôi cũng khuyến khích độc giả suy ngẫm về lý do tại sao những khác biệt này tồn tại và liệu chúng có thể được tận dụng để cải thiện cả hai phía hay không.

**Hạt nhân.** Như đã đề cập ở trên, các hạt nhân riêng biệt được sử dụng bởi hai module đặt ra một câu hỏi thú vị: tại sao mạng truyền thẳng (FFN) lại áp dụng hạt nhân ReLU thay vì một hạt nhân chính xác hơn như hạt nhân Exp?

Chúng tôi đề xuất giả thuyết sau: **Một hạt nhân có độ chính xác truy xuất thấp hơn khuyến khích bộ nhớ khóa-giá trị đa nghĩa hơn: nhiều sự kiện không liên quan có thể được lưu trữ dưới cùng một không gian khóa.** Điều này thường được gọi là chồng chập (superposition) [9, 14] trong nghiên cứu về khả năng diễn giải. Điều này mở rộng lượng kiến thức trừu tượng, nén được lưu vào một bộ nhớ liên kết có độ rộng cố định. Ngược lại, các lớp Attention thường xuyên cần xác định chính xác một token cụ thể từ ngữ cảnh hiện tại (ví dụ: induction heads [25]), và do đó được hưởng lợi từ một hạt nhân có độ chính xác cao, đơn nghĩa hơn. Ngược lại, FFN phải chưng cất toàn bộ corpus huấn luyện thành các trọng số tĩnh; khả năng chồng chập nhiều mảnh thông tin dưới một vector do đó là có lợi, ngay cả khi phải trả giá bằng việc truy xuất nhiễu hơn.

Một bằng chứng thực nghiệm đến từ việc Anthropic thay thế ReLU bằng kích hoạt Softmax Linear Unit (SoLU) [8]:

$$\kappa_{\text{SoLU}}(\mathbf{x}, \mathbf{y}) = (\mathbf{x}^{\top}\mathbf{y}) \exp\left(\frac{\mathbf{x}^{\top}\mathbf{y}}{\tau}\right), \quad (16)$$

có thể được xem như một sự pha trộn nhân giữa các hạt nhân tuyến tính và hàm mũ. Họ quan sát thấy rằng các biểu diễn ẩn trở nên **rõ ràng đơn nghĩa**, nhưng độ chính xác của mô hình trên benchmark TriviaQA nặng về kiến thức lại suy giảm.

Trong Phụ lục A, chúng tôi suy ra SNR nghịch đảo cho hạt nhân SoLU:

$$\boxed{\text{SNR}_{\text{SoLU}}^{-1} \approx \frac{5N}{d_k \exp(2\sqrt{d_k})}.} \quad (17)$$

Điều này cho thấy SoLU cung cấp độ chính xác truy xuất cao hơn ReLU, phù hợp với hành vi đơn nghĩa được quan sát của nó. Cùng nhau, những phát hiện này hỗ trợ giả thuyết rộng hơn của chúng tôi: lựa chọn hạt nhân về cơ bản **điều tiết sự đánh đổi giữa mật độ kiến thức (lượng thông tin được lưu trữ) và độ chính xác truy xuất (tính cụ thể của việc truy cập bộ nhớ)** trong các kiến trúc neural. Chúng tôi để lại việc khám phá đầy đủ hơn về lựa chọn hạt nhân cho việc lưu giữ kiến thức so với nhớ lại ngữ cảnh cho các nghiên cứu trong tương lai.

**Chuẩn hóa.** So với công thức nhớ lại liên kết được giới thiệu trước đó (Phương trình 9), Softmax Attention tiêu chuẩn bao gồm một yếu tố chuẩn hóa bổ sung:

$$\mathbf{S}_t\phi(\mathbf{q}_t) = \frac{\sum_{i=1}^{t} \mathbf{v}_i \exp(\mathbf{q}_t^{\top}\mathbf{k}_i)}{\sum_{i=1}^{t} \exp(\mathbf{q}_t^{\top}\mathbf{k}_i)}, \quad (18)$$

mà FFN bỏ qua. Công trình liên quan chặt chẽ nhất với chúng tôi về việc tích hợp chuẩn hóa vào FFN là NormFormer [37], giới thiệu phép toán LayerNorm ngay sau kích hoạt phi tuyến (hàm hạt nhân), dẫn đến cải thiện tính ổn định huấn luyện. Vì nhiều biến thể linear-attention [20, 40, 48] đã chỉ ra rằng yếu tố này có thể được thay thế bằng một chuẩn hóa đơn giản cho mỗi đầu, chúng tôi coi `chuẩn hóa chủ yếu là một cơ chế cho tính ổn định huấn luyện` hơn là một yêu cầu thiết kế cơ bản.

**Đa đầu.** Attention đa đầu chia **q**, **k**, **v** thành n_h đầu và sử dụng ma trận đầu ra **W**_O ∈ ℝ^(d×d_h n_h) để kết hợp kết quả từng đầu:

$$[\mathbf{q}_{j,1}; \mathbf{q}_{j,2}; \ldots; \mathbf{q}_{j,n_h}] = \mathbf{q}_t, \quad (19)$$
$$[\mathbf{k}_{i,1}; \mathbf{k}_{i,2}; \ldots; \mathbf{k}_{i,n_h}] = \mathbf{k}_i, \quad (20)$$
$$[\mathbf{v}_{i,1}; \mathbf{v}_{i,2}; \ldots; \mathbf{v}_{i,n_h}] = \mathbf{v}_i, \quad (21)$$

$$\mathbf{o}_{t,:} = \sum_{i=1}^{t} \mathbf{v}_{i,:} \kappa(\mathbf{q}_{t,:}, \mathbf{k}_{i,:}), \quad (22)$$
$$\mathbf{o}_t = \mathbf{W}_O[\mathbf{o}_{t,1}; \mathbf{o}_{t,2}; \ldots; \mathbf{o}_{t,n_h}]. \quad (23)$$
  
Cơ chế chia nhỏ này cho phép multihead attention tính toán n_h mẫu hình tương đồng khác nhau cho cùng một cặp query-key. Như trong phân tích trước đó về hạt nhân hàm mũ, cơ chế multihead có thể đánh đổi độ chính xác truy xuất để lấy khả năng biểu đạt khi sử dụng các hạt nhân mạnh hơn, hoặc ngược lại, tăng cường chồng chập để cải thiện dung lượng kiến thức. Hơn nữa, từ góc độ trực quan, ma trận chiếu đầu ra **𝑾_O trong multi-head attention có thể giảm nhiễu** trong Phương trình 4. Ví dụ, trong Multi-Query Attention (MQA), nếu 𝑾_O học cách tính trung bình đơn giản trên n_h đầu với keys và values được chia sẻ, phương sai của nhiễu sẽ giảm đi một hệ số n_h so với single-head attention có cùng chiều key/value.

⁷Ví dụ, trong Multi-Query Attention (MQA), nếu **W**_O học cách tính trung bình đơn giản trên n_h đầu với khóa và giá trị được chia sẻ, phương sai của nhiễu được giảm đi hệ số n_h, so với attention đơn đầu có cùng số chiều khóa/giá trị.

Trong bài báo Transformer gốc, các tác giả đã thử nghiệm thay thế toàn bộ lớp FFN bằng multihead attention[42], quan sát thấy một số cải thiện hiệu suất. Với việc attention đã được tối ưu hóa đáng kể trên GPU hiện đại[6,5,34], chúng tôi cho rằng việc đưa cơ chế multihead vào FFN, cụ thể là chia nhỏ 𝒙,𝑾_Ki,𝑾_Vi trong Phương trình 14, xứng đáng được xem xét lại và chúng tôi để việc khám phá ý tưởng này cho nghiên cứu tương lai.

**Thưa (Sparsity).** Dễ dàng thấy rằng khi số lượng khóa và giá trị trở nên lớn, các khóa liên quan đến mỗi truy vấn chắc chắn sẽ thưa. Cách tiếp cận đơn giản nhất để tích hợp tính thưa vào FFN là thông qua cơ chế Mixture-of-Experts (MoE) [11, 36]. Xét một FFN với E mạng con chuyên gia, mỗi mạng được tham số hóa bởi các ma trận trọng số riêng biệt **W**_K^(e), **W**_V^(e) ∈ ℝ^(m/E)×d. Một hàm cổng g_e(**x**) (thường thưa) định tuyến đầu vào đến một tập con nhỏ (thường là một) của các chuyên gia này:

$$\text{FFN}_{\text{MoE}}(\mathbf{x}) = \sum_{e=1}^{E} g_e(\mathbf{x}) \left( \sum_{i \in \text{Expert}(e)} \mathbf{W}_{V_i}^{(e)} \text{ReLU}(\mathbf{W}_{K_i}^{(e)} \mathbf{x}) \right). \quad (24)$$

Để đạt được tính thưa tương tự trong Attention, người ta có thể phân chia các cặp khóa-giá trị thành các tập con chuyên gia rời rạc và thực hiện định tuyến thưa dựa trên truy vấn:

$$\mathbf{o}_{t,\text{MoE}} = \sum_{e=1}^{E} g_e(\mathbf{q}_t, \{\mathbf{k}_i\}_{i \in \text{Expert}(e)}) \left( \sum_{i \in \text{Expert}(e)} \mathbf{v}_i \kappa(\mathbf{q}_t, \mathbf{k}_i) \right). \quad (25)$$

Sự khác biệt cốt lõi giữa Attention và FFN trong cơ chế MoE là các khóa và giá trị (**k** và **v**) trong Attention là động, trong khi các tham số chuyên gia trong FFN là tĩnh. Do đó, hàm cổng g_e trong Attention cần phụ thuộc vào tập các vector khóa {**k**_i}_i∈Expert(e). Ví dụ, kiến trúc MoBA [22] định nghĩa hàm cổng như sau (các điều khoản chuẩn hóa được bỏ qua để rõ ràng):

$$g_e(\mathbf{q}_t, \{\mathbf{k}_i\}_{i \in \text{Expert}(e)}) = \begin{cases} 1 & s_e(\mathbf{q}_t) \in \text{TopK}(\{s_{e'}(\mathbf{q}_t) | e' \in [E]\}, k) \\ 0 & \text{otherwise} \end{cases}, \quad (26)$$

$$s_e(\mathbf{q}_t) = \langle\mathbf{q}_t, \text{mean-pool}(\{\mathbf{k}_i\}_{i \in \text{Expert}(e)})\rangle. \quad (27)$$

**Cổng (Gating).** Một lựa chọn kiến trúc phổ biến khác trong FFN là cổng, như được minh họa bởi hàm kích hoạt SwiGLU [35]. Cổng SwiGLU giới thiệu các tương tác nhân giữa hai phép biến đổi tuyến tính của đầu vào, được định nghĩa như sau:

$$\text{FFN}_{\text{SwiGLU}}(\mathbf{x}) = \mathbf{W}_V^{\top} \left( (\mathbf{W}_G \mathbf{x}) \odot \text{Swish}(\mathbf{W}_U \mathbf{x}) \right), \quad (28)$$

trong đó **W**_K, **W**_G, **W**_V ∈ ℝ^(m×d) là các ma trận tham số, ⊙ biểu thị phép nhân theo từng phần tử, và Swish được cho bởi Swish(z) = z · Sigmoid(z).

Được biểu diễn trong khung bộ nhớ liên kết, SwiGLU có thể được viết lại như sau:

$$\text{FFN}_{\text{SwiGLU}}(\mathbf{x}) = \sum_{i=1}^{m} \underbrace{\mathbf{W}_{V_i}}_{\mathbf{v}_i} \underbrace{(\mathbf{W}_{G_i}^{\top} \mathbf{x})}_{g_i(\mathbf{x})} \text{Swish}\underbrace{(\mathbf{W}_{K_i}^{\top} \mathbf{x})}_{\kappa(\cdot,\cdot) \quad \mathbf{k}_i \quad \mathbf{q}_t}, \quad (29)$$

trong đó (**W**_{G_i}^⊤ **x**) là số hạng cổng được giới thiệu thêm g_i(**x**), có thể khuếch đại hoặc triệt tiêu phản hồi của truy vấn với khóa cụ thể. Một cách trực quan, cơ chế cổng này cung cấp một điều chế động, phụ thuộc vào đầu vào. Các transformer tuyến tính gần đây đã thử nghiệm với các cơ chế cổng trong các lớp attention, chẳng hạn như Gated Linear Attention [47]. Số hạng cổng được định nghĩa như sau (các điều khoản chuẩn hóa được bỏ qua):

$$\mathbf{o}_{t,\text{gated}} = \sum_{i=1}^{t} \mathbf{v}_i g_t(\mathbf{x}_{i:t}) \kappa(\mathbf{q}_t, \mathbf{k}_i), \quad (30)$$

$$g_t(\mathbf{x}_{i:t}) = \prod_{j=i+1}^{t} \alpha_j \quad (31)$$

trong đó α_j được tham số hóa thông qua một lớp tuyến tính theo sau là sigmoid trên trạng thái ẩn **x**_j. Một công trình gần đây hơn, FoX [21], cũng tích hợp cổng vào Softmax attention, áp dụng một công thức về cơ bản giống với cách chúng tôi trình bày ở đây.

Thú vị là, các cơ chế cổng trong attention thường được diễn giải như một dạng cơ chế quên và do đó thường được biểu diễn dưới dạng tích lũy của các giá trị cổng từ 0 đến 1 (được thực hiện thông qua sigmoid).

Tuy nhiên, khi quan sát cơ chế cổng trong FFN, chúng tôi nhận thấy sự vắng mặt của các cơ chế tích lũy như vậy. Chúng tôi đưa ra giả thuyết rằng việc định nghĩa hàm cổng đơn giản là gi(xi:t)=WG(xi)⊤xt có thể đạt được hiệu ứng tương tự trong khi dễ triển khai hơn. Chúng tôi để dành việc kiểm chứng giả thuyết này cho các nghiên cứu trong tương lai. Về lý do tại sao cơ chế cổng nói chung lại hiệu quả, chúng tôi khuyến khích tiếp tục nghiên cứu và khám phá các nguyên lý nền tảng cùng các giải thích lý thuyết của chúng.

## 2.2 Cập nhật Bộ nhớ

Bộ nhớ liên kết được cập nhật như thế nào? Ví dụ, bằng cách viết lại Phương trình 1, ta có:

$$\mathbf{S}_t = \underbrace{\left(\sum_{i=1}^{t-1} \mathbf{v}_i\mathbf{k}_i^{\top}\right)}_{\mathbf{S}_{t-1}} + \mathbf{v}_t\mathbf{k}_t^{\top}$$

$$= \mathbf{S}_{t-1} + \mathbf{v}_t\mathbf{k}_t^{\top}. \quad (32)$$

Điều này cho thấy, đối với bộ nhớ liên kết cổ điển, việc cập nhật tại bước thời gian $t$ được thực hiện trực tiếp bằng cách thêm tích ngoài của các vector mới nhất $\mathbf{v}_t$ và $\mathbf{k}_t$. Chúng tôi thường gọi Phương trình 32 là dạng truy hồi của bộ nhớ liên kết cổ điển. Nếu chúng ta diễn giải phương trình này như một cập nhật giảm gradient một bước trên $\mathbf{S}$ (bỏ qua tốc độ học), ta có:

$$\mathbf{S}_t = \mathbf{S}_{t-1} - \underbrace{(-\mathbf{v}_t\mathbf{k}_t^{\top})}_{\frac{\partial\mathcal{L}_t}{\partial\mathbf{S}_{t-1}}}, \quad (33)$$

trong đó $\mathcal{L}_t$ biểu diễn mục tiêu tối ưu tương ứng với việc cập nhật bộ nhớ, có thể được suy ra một cách rõ ràng:

$$\mathcal{L}_t(\mathbf{S}_{t-1}) = -\langle\mathbf{S}_{t-1}\mathbf{k}_t, \mathbf{v}_t\rangle, \quad (34)$$

với $\langle\cdot,\cdot\rangle$ biểu thị tích trong. Một cách trực quan, mục tiêu này nhằm cập nhật $\mathbf{S}$ sao cho việc nhớ lại $\mathbf{v}_t$ từ bộ nhớ đã cập nhật sử dụng $\mathbf{k}_t$ hiệu quả nhất có thể. Tuy nhiên, một nhược điểm rõ ràng của mục tiêu này là, ngoài việc thúc đẩy tích trong lớn giữa $\mathbf{S}\mathbf{k}_t$ và $\mathbf{v}_t$, nó cũng khuyến khích chuẩn của $\mathbf{S}\mathbf{k}_t$ tăng lên không giới hạn, có thể gây ra bất ổn định số. Trong thực tế, vấn đề này có thể được giảm thiểu bằng cách, ví dụ, giới thiệu các cơ chế suy giảm trên $\mathbf{S}$ [47]. Các phương pháp cải tiến khác nhau để cập nhật bộ nhớ sẽ được thảo luận chi tiết dưới đây.

Để tạo điều kiện cho các thảo luận về các phương pháp cập nhật bộ nhớ khác nhau, trước tiên chúng tôi định nghĩa một dạng truy hồi tổng quát của bộ nhớ liên kết như sau:

$$\mathbf{S}_t = \mathbf{A}_t\mathbf{S}_{t-1}\mathbf{B}_t + \mathbf{C}_t, \quad (35)$$

trong đó $\mathbf{A}_t$, $\mathbf{B}_t$, và $\mathbf{C}_t$ là các ma trận tham số. Ví dụ, Phương trình 32 là một trường hợp đặc biệt của Phương trình 35 khi $\mathbf{A}_t = \mathbf{I}$, $\mathbf{B}_t = \mathbf{I}$, và $\mathbf{C}_t = \mathbf{v}_t\mathbf{k}_t^{\top}$. Mục tiêu tối ưu nào tương ứng với Phương trình 35? Tham khảo Phụ lục B.1, chúng ta có thể xây dựng hàm mục tiêu liên quan như sau:


$$\mathcal{L}_t(\mathbf{S}_{t-1}) = \frac{1}{2}\text{tr}(\mathbf{S}_t^{\top}\mathbf{S}_{t-1}) - \frac{1}{2}\text{tr}(\mathbf{S}_{t-1}^{\top}\mathbf{A}_t\mathbf{S}_{t-1}\mathbf{B}_t) - \text{tr}(\mathbf{C}_t^{\top}\mathbf{S}_{t-1}), \quad (36)$$

trong đó $\text{tr}(\cdot)$ biểu thị phép toán vết (trace). Lưu ý rằng cấu trúc này có một ràng buộc: các ma trận $\mathbf{A}_t$ và $\mathbf{B}_t$ phải đối xứng. Bằng cách chỉ định các tổ hợp khác nhau của $\mathbf{A}_t$, $\mathbf{B}_t$, và $\mathbf{C}_t$, chúng ta thu được các dạng truy hồi riêng biệt và các mục tiêu tối ưu tương ứng. Mỗi tổ hợp tương ứng với một mô hình bộ nhớ liên kết mới, cho phép chúng ta diễn giải các mô hình này một cách rõ ràng từ góc độ cập nhật bộ nhớ.

Bảng 2 tóm tắt các mô hình bộ nhớ liên kết khác nhau, các ma trận tham số tương ứng của chúng ($\mathbf{A}_t$, $\mathbf{B}_t$, và $\mathbf{C}_t$) trong dạng truy hồi (Phương trình 35), và các mục tiêu tối ưu $\mathcal{L}_t(\mathbf{S}_{t-1})$.

**Bảng 2** Các dạng cập nhật bộ nhớ khác nhau. Các mô hình bộ nhớ liên kết khác nhau, các ma trận tham số tương ứng của chúng ($\mathbf{A}_t$, $\mathbf{B}_t$, và $\mathbf{C}_t$) trong dạng truy hồi (Phương trình 35), và các mục tiêu tối ưu $\mathcal{L}_t(\mathbf{S}_{t-1})$. Các phép suy dẫn chi tiết có thể tìm thấy trong Phụ lục B.2.

| Mô hình | $\mathbf{A}_t$ | $\mathbf{B}_t$ | $\mathbf{C}_t$ | $\mathcal{L}_t(\mathbf{S}_{t-1})$ |
|---------|----------------|----------------|----------------|-----------------------------------|
| Linear Attention [17] | $\mathbf{I}$ | $\mathbf{I}$ | $\mathbf{v}_t\mathbf{k}_t^{\top}$ | $-\langle\mathbf{S}_{t-1}\mathbf{k}_t, \mathbf{v}_t\rangle$ |
| Gated Linear Attention [47] | $\text{diag}(\boldsymbol{\lambda}_t)$ | $\mathbf{I}$ | $\mathbf{v}_t\mathbf{k}_t^{\top}$ | $-\langle\mathbf{S}_{t-1}\mathbf{k}_t, \mathbf{v}_t\rangle$ + $\frac{1}{2}\|\|\text{diag}(\sqrt{1-\boldsymbol{\lambda}_t})\mathbf{S}_{t-1}\|\|_F^2$ |
| DeltaNet [33, 48] | $\mathbf{I}$ | $\mathbf{I} - \mathbf{k}_t\mathbf{k}_t^{\top}$ | $\mathbf{v}_t\mathbf{k}_t^{\top}$ | $\frac{1}{2}\|\|\mathbf{S}_{t-1}\mathbf{k}_t - \mathbf{v}_t\|\|^2$ |
| DeltaNet + Momentum | $\mathbf{I}$ | $\mathbf{I} - \mathbf{k}_t\mathbf{k}_t^{\top}$ | $\eta_t\mathbf{C}_{t-1} + \mathbf{v}_t\mathbf{k}_t^{\top} = \sum_{i=1}^{t}\left(\prod_{j=i+1}^{t}\eta_j\right)\mathbf{v}_i\mathbf{k}_i^{\top}$ | $\frac{1}{2}\|\|\mathbf{S}_{t-1}\mathbf{k}_t\|\|^2$ - $\sum_{i=1}^{t}\left(\prod_{j=i+1}^{t}\eta_j\right)\mathbf{v}_i^{\top}\mathbf{S}_{t-1}\mathbf{k}_i$ |
| Softmax Attention w/o Norm | $\mathbf{I}$ | $\mathbf{I}$ | $\mathbf{v}_t\phi(\mathbf{k}_t)^{\top}$ | $-\langle\mathbf{S}_{t-1}\phi(\mathbf{k}_t), \mathbf{v}_t\rangle$ |
| Softmax Attention w/ Norm* [43] | $\frac{t-1}{t}\mathbf{I}$ | $\mathbf{I}$ | $\frac{1}{t}\mathbf{v}_t\phi(\mathbf{k}_t)^{\top}$ | $-\frac{1}{t}\langle\mathbf{S}_{t-1}\phi(\mathbf{k}_t), \mathbf{v}_t\rangle$ + $\frac{1}{2t}\|\|\mathbf{S}_{t-1}\|\|_F^2$ |
| Gated Softmax Attention* [21] | $\frac{t-1}{t}\text{diag}(\boldsymbol{\lambda}_t)$ | $\mathbf{I}$ | $\frac{1}{t}\mathbf{v}_t\phi(\mathbf{k}_t)^{\top}$ | $-\frac{1}{t}\langle\mathbf{S}_{t-1}\phi(\mathbf{k}_t), \mathbf{v}_t\rangle$ + $\frac{1}{2t}\|\|\text{diag}(\sqrt{1-\boldsymbol{\lambda}_t})\mathbf{S}_{t-1}\|\|_F^2$ |

Mọi phần tử của $\boldsymbol{\lambda}$ và $\eta \in (0, 1)$.

"Softmax Attention" chỉ sự chú ý softmax nhân quả vanilla, không bao gồm các biến thể như sự chú ý softmax thưa. "Norm" biểu thị yếu tố chuẩn hóa của phép toán softmax: $\sum_i \exp(\mathbf{q}_t^{\top}\mathbf{k}_i)$. Ánh xạ đặc trưng của sự chú ý softmax $\phi(\cdot)$ là Phương trình 11.

\* $t$ đủ lớn.

Bảng 2 tóm tắt các mô hình bộ nhớ liên kết khác nhau, các ma trận tham số tương ứng (𝑨t, 𝑩t, và 𝑪t) ở dạng truy hồi (Phương trình 35), và các mục tiêu tối ưu ℒt(𝑺t-1).

* `Linear Attention`. Mục tiêu tối ưu của linear attention không bị giới hạn, có thể dẫn đến bất ổn định số do chuẩn của 𝑺𝒌 tăng vô hạn. Gated linear attention [47] giới thiệu một thành phần suy giảm bộ nhớ diag(𝝀), tương ứng với việc thêm một thành phần điều chuẩn vào chuẩn Frobenius của 𝑺 trong mục tiêu tối ưu. Điều này giảm thiểu vấn đề chuẩn tăng vô hạn với cái giá là quên bớt bộ nhớ.

* `DeltaNet` [33, 48] thay thế hàm `mục tiêu tích trong` bằng mục tiêu hồi quy L2, tự nhiên áp đặt điều chuẩn lên 𝑺𝒌. Ở dạng truy hồi, điều này tương ứng với việc giới thiệu một thành phần suy giảm mới 𝑰−𝒌𝒌⊤. Tác dụng của suy giảm này là truy xuất và làm yếu các giá trị lịch sử liên quan đến các khóa tương tự trước khi ghi vào bộ nhớ, từ đó ngăn chặn sự bùng nổ chuẩn sau các cập nhật. Ngoài ra, động lượng bậc nhất có thể được kết hợp vào các cập nhật cặp khóa-giá trị để điều chỉnh động lực cập nhật bộ nhớ. Behrouz et al. [2] phát hiện rằng việc kết hợp động lượng tác động tích cực đến hiệu suất mô hình. Mô hình trong [2] không hoàn toàn tương đương với DeltaNet; do đó phát hiện này chỉ nên được xem như một tham khảo.

* `Softmax attention` [43] cải thiện hiệu suất truy xuất bộ nhớ bằng cách giới thiệu một kernel hàm mũ. Không có thành phần chuẩn hóa trong phép toán softmax, mục tiêu tối ưu vẫn không bị giới hạn. Với thành phần chuẩn hóa, đối với ngữ cảnh đủ dài (t lớn), theo các suy dẫn trong Phụ lục 7.2, mục tiêu bao gồm một thành phần điều chuẩn trên chuẩn Frobenius của 𝑺, giúp giảm bất ổn định số. Ngoài ra, mục tiêu xấp xỉ giới thiệu một hệ số 1/t. Khi t tăng, 1/t → 0, khiến ℒt → 0 và do đó dẫn đến sự biến mất dần của gradient đối với 𝑺. Điều này có thể ức chế các cập nhật bộ nhớ (được thảo luận chi tiết trong Mục 2.2.2). Tương tự như gated linear attention, chúng ta cũng có thể giới thiệu sự suy giảm bộ nhớ cho softmax attention bằng cách kết hợp một hệ số cổng diag(𝝀t) vào ma trận tham số 𝑨t. Lin et al. [21] phát hiện rằng điều này cải thiện hiệu suất của softmax attention.

####  2.2.1 Minh họa 2: Softmax Attention với Delta Rule

Từ so sánh các mô hình bộ nhớ liên kết trong Bảng 2, ta rút ra hai điểm chính:

* _Cách cải thiện độ chính xác truy xuất bộ nhớ?_ Một phương pháp hiệu quả là sử dụng kernel trick. Ví dụ, softmax attention dùng hàm kernel mũ. Như đã thảo luận ở Mục 2.1.1, phương pháp này nâng cao độ chính xác truy xuất mà không tăng độ phức tạp không gian.

* _Cách quản lý cập nhật bộ nhớ hiệu quả để tránh bùng nổ chuẩn phổ của 𝑺?_ Có thể chứng minh rằng các mô hình dạng 𝑺ₜ=𝑺ₜ₋₁+𝒗ᵢ𝒌ᵢᵀ sẽ làm tăng chuẩn phổ của 𝑺 theo thời gian, gây bất ổn số, bất kể có dùng kernel trick hay không.

Softmax attention giải quyết vấn đề này bằng cách thêm thành phần chuẩn hóa, tương đương với việc giảm dần thông tin mới 𝒗ₜ theo hệ số 1/t. Điều này `ngụ ý thông tin càng gần tương lai càng bị suy giảm` (1/t 𝒗ₜ𝒌ₜᵀ → 0), làm chậm tăng trưởng chuẩn phổ của 𝑺. Tuy nhiên, giả định thông tin gần tương lai ít quan trọng hơn là không hợp lý. Thông thường, thông tin cũ thường ít quan trọng hơn. Do đó, đảo ngược hướng suy giảm sẽ hợp lý hơn. Gated linear attention thực hiện điều này bằng cách thêm hệ số suy giảm mũ diag(𝝀).

Tuy nhiên, thông tin cũ không phải lúc nào cũng ít liên quan. Cách tiếp cận thông minh hơn là đánh giá sự trùng lặp giữa giá trị mới 𝒗ₜ với thông tin lịch sử, xóa phần trùng lặp trước khi ghi vào bộ nhớ. Chiến lược này gọi là delta rule, chính là cơ chế cập nhật bộ nhớ của DeltaNet.

Từ đó, _liệu có thể đề xuất mô hình bộ nhớ liên kết mới kết hợp ưu điểm của cả kernel trick và delta rule?_ Mô hình này sẽ quản lý cập nhật bộ nhớ thông minh hơn đồng thời đảm bảo độ chính xác truy xuất cao.

Để đạt được điều này, cách đơn giản là đưa delta rule vào softmax attention. Như Bảng 2 cho thấy, cốt lõi của delta rule là thành phần suy giảm 𝑰−𝒌𝒌ᵀ trong dạng truy hồi. Do đó, thay thế 𝑩ₜ trong dạng truy hồi của softmax attention và kết hợp kernel trick sẽ thành công đưa delta rule vào softmax attention:

$$\mathbf{S}_t = \underbrace{\mathbf{S}_{t-1}\left(\mathbf{I} - \phi(\mathbf{k}_t)\phi(\mathbf{k}_t)^{\top}\right)}_{\text{Suy giảm theo quy tắc Delta}} + \mathbf{v}_t\phi(\mathbf{k}_t)^{\top}. \quad (37)$$

Để đơn giản, số hạng chuẩn hóa của phép toán softmax được bỏ qua ở đây mà không ảnh hưởng đến phép suy dẫn⁹. Trong thực tế, số hạng chuẩn hóa vẫn được giữ lại để tránh bất ổn định số.

⁹Như được chỉ ra trong Bảng 2, số hạng chuẩn hóa của phép toán softmax chia tỷ lệ $\mathbf{A}_t$ và $\mathbf{C}_t$ trong dạng truy hồi theo hệ số liên quan đến $\frac{1}{t}$ khi $t$ trở nên đủ lớn. Hệ số tỷ lệ này độc lập và không ảnh hưởng đến phép suy dẫn chính.

Phương trình 37 có vẻ phức tạp. Liệu có thể xây dựng một mô hình bộ nhớ liên kết tương đương chính xác với mô hình được biểu diễn bởi Phương trình 37, nhưng có dạng truy hồi đơn giản hơn không? Lý tưởng nhất, dạng truy hồi nên giống với softmax attention đã căn chỉnh, tức là, $\mathbf{A}_t = \mathbf{B}_t = \mathbf{I}$, trong khi $\mathbf{C}_t$ vẫn là ma trận tích ngoài. Để đạt được điều này, chúng tôi giới thiệu một biến mới $\mathbf{u}_t$, phụ thuộc vào lịch sử $\{\mathbf{k}\}_{i=1}^t$ và $\{\mathbf{v}\}_{i=1}^t$. Do đó, chúng tôi xây dựng một công thức truy hồi mới:

$$\mathbf{S}_t = \mathbf{S}_{t-1} + \mathbf{u}_t\phi(\mathbf{k}_t)^{\top}. \quad (38)$$

Để đảm bảo sự tương đương giữa mô hình bộ nhớ này và mô hình được định nghĩa bởi Phương trình 37, chúng tôi giải tìm $\mathbf{u}_t$ bằng phương pháp hệ số bất định (xem Phụ lục C để biết chi tiết). Điều này cho ra điều kiện sau cho $\mathbf{u}_t$:


$$\mathbf{u}_t = -\sum_{i=1}^{t-1} \phi(\mathbf{k}_i)^{\top}\phi(\mathbf{k}_t)\mathbf{u}_i + \mathbf{v}_t$$

$$= -\sum_{i=1}^{t-1} \exp(\mathbf{k}_i^{\top}\mathbf{k}_t)\mathbf{u}_i + \mathbf{v}_t. \quad (39)$$

So với softmax attention, $\mathbf{v}_t$ giờ được "thay thế" bởi $\mathbf{u}_t$ để tạo thành tích ngoài với $\phi(\mathbf{k}_t)$ trước khi được ghi vào bộ nhớ. Một cách trực quan, từ Phương trình 39, $\mathbf{u}_t$ được suy ra từ $\mathbf{v}_t$ thông qua một bước "xử lý" bổ sung. Cụ thể, độ tương đồng giữa $\mathbf{k}_t$ và các khóa lịch sử được tính toán trước để tạo trọng số cho tổng của các $\{\mathbf{u}_i\}_{i=1}^{t-1}$ lịch sử, cho ra $-\sum_{i=1}^{t-1} \exp(\mathbf{k}_i^{\top}\mathbf{k}_t)\mathbf{u}_i$. Số hạng này biểu diễn thông tin trong bộ nhớ trùng lặp với $\mathbf{v}_t$. Chúng ta loại bỏ thông tin "dư thừa" này khỏi $\mathbf{v}_t$ để thu được $\mathbf{u}_t$. Quá trình này chính xác là quy tắc delta.

Sau khi bộ nhớ $\mathbf{S}$ được cập nhật, phương pháp nhớ lại bộ nhớ tương ứng vẫn nhất quán với dạng của softmax attention:

$$\mathbf{o}_t = \mathbf{S}_t\phi(\mathbf{q}_t) = \sum_{i=1}^{t} \exp(\mathbf{q}_t^{\top}\mathbf{k}_i)\mathbf{u}_i. \quad (40)$$

Tóm lại, mô hình mới thể hiện những thay đổi hình thức tối thiểu so với softmax attention, chỉ đơn thuần thay thế $\mathbf{v}$ gốc bằng $\mathbf{u}$ sau một bước "xử lý" bổ sung theo quy tắc delta. Thông qua sửa đổi nhẹ này, chúng tôi đã tích hợp thành công quy tắc delta vào softmax attention.

**Dạng Tổng quát.** Chúng ta thực sự có thể xây dựng mô hình mới ở dạng tổng quát hơn bằng cách sửa đổi các thành phần sau:

• **Hàm hạt nhân.** Các hàm hạt nhân được sử dụng để tính toán $\mathbf{u}$ (Phương trình 39) và để truy xuất bộ nhớ (Phương trình 40) không cần phải giống nhau; các hạt nhân khác nhau có thể được áp dụng độc lập.

• **Cổng.** Cổng có thể được giới thiệu riêng biệt vào hai phần của việc tính toán $\mathbf{u}$ (Phương trình 39): số hạng $-\sum_{i=1}^{t-1} \phi(\mathbf{k}_i)^{\top}\phi(\mathbf{k}_t)\mathbf{u}_i$ và vector $\mathbf{v}_t$.

• **Tính toán độ tương đồng.** Trong việc tính toán $\mathbf{u}$ (Phương trình 39), một vector mới, thay vì $\mathbf{k}_t$, có thể được giới thiệu để tính toán độ tương đồng với $\{\mathbf{k}_i\}_{i=1}^{t-1}$, từ đó kiểm soát việc ghi và xóa bộ nhớ.

Tóm lại, dạng tổng quát của mô hình mới có thể được viết như sau:

$$\mathbf{u}_t = \alpha_t\mathbf{v}_t - \beta_t\sum_{i=1}^{t-1} \kappa_1(\mathbf{k}_i, \mathbf{w}_t)\mathbf{u}_i, \quad (41)$$

$$\mathbf{o}_t = \sum_{i=1}^{t} \kappa_2(\mathbf{k}_i, \mathbf{q}_t)\mathbf{u}_i, \quad (42)$$

trong đó $\kappa_1(\cdot,\cdot)$ và $\kappa_2(\cdot,\cdot)$ là các hàm hạt nhân độc lập; $\alpha_t$ và $\beta_t$ là các cổng độc lập; và $\mathbf{w}_t$ là vector truy xuất mới được giới thiệu. Và chúng tôi gọi mô hình được đặc trưng bởi dạng tổng quát này là **DeltaFormer**.

Trên thực tế, cả Transformer và DeltaNet đều là các trường hợp đặc biệt của dạng tổng quát trên. Cụ thể, nếu chúng ta đặt $\beta_t = 0$, $\alpha_t = 1$, và sử dụng $\mathbf{o}_t = \frac{\sum_{i=1}^{t} \exp(\mathbf{k}_i^{\top}\mathbf{q}_t/\sqrt{d})\mathbf{u}_i}{\sum_{i=1}^{t} \exp(\mathbf{k}_i^{\top}\mathbf{q}_t/\sqrt{d})}$, chúng ta khôi phục lại Transformer tiêu chuẩn. Nếu chúng ta đặt $\alpha_t = 1$, $\mathbf{w}_t = \mathbf{k}_t$, và cả $\kappa_1(\cdot,\cdot)$ và $\kappa_2(\cdot,\cdot)$ đều là hạt nhân tích trong tuyến tính, chúng ta khôi phục chính xác DeltaNet.

##### Khả năng biểu diễn của mô hình.

Về bản chất, mạng neural có thể xem như các mạch điện. Khả năng biểu diễn của chúng được đo bằng độ phức tạp mạch - nghiên cứu kích thước hoặc **độ sâu tối thiểu** cần thiết để giải quyết các bài toán tính toán, từ đó đặc trưng cho độ khó tính toán nội tại.

Mạch Boolean là đồ thị có hướng không chu trình gồm các cổng logic, được định nghĩa bởi 4 tham số: 
1) `Kích thước`: tổng số cổng
2) `Độ sâu`: số cổng trên đường dài nhất từ đầu vào đến đầu ra  
3) `Fan-in`: số đầu vào tối đa mỗi cổng (có thể giới hạn hoặc không giới hạn)
4) `Loại cổng`: thường là AND, OR, NOT

Các mạch được phân loại thành 3 lớp chính:
* _NCᵏ_: Kích thước đa thức, độ sâu O(logᵏn), fan-in giới hạn. Ví dụ: cộng số nguyên (k=1), nhân ma trận (k=2).
* _ACᵏ_: Tương tự NCᵏ nhưng fan-in không giới hạn. Có thể thực hiện các phép logic đơn giản như "có đầu vào nào bằng 1?".
* _TCᵏ_: Sử dụng cổng ngưỡng (đếm số đầu vào bằng 1), fan-in không giới hạn. Mạnh hơn, có thể thực hiện so sánh số, đếm, biểu quyết đa số.

Hệ thống phân cấp giữa các lớp mạch:
| NC⁰⊂AC⁰⊂TC⁰⊆NC¹⊆AC¹⊆TC¹⊆NC²⊆⋯⊆P. | (43)

Ví dụ minh họa:
* _AC⁰ vs TC⁰_: AC⁰ không thể tính tổng n số trong một bước do thiếu cổng ngưỡng, trong khi TC⁰ có thể.
* _TC⁰ vs NC¹_: TC⁰ không thể hoán đổi phần tử trong chuỗi độ dài tùy ý do độ sâu cố định, trong khi NC¹ có thể nhờ cấu trúc cây nhị phân.

Với Transformer, nghiên cứu cho thấy phiên bản độ sâu cố định không có chain-of-thought chỉ giải được bài toán trong TC⁰. Trong khi đó, DeltaNet vượt qua TC⁰. Bằng chứng DeltaFormer có thể theo dõi n phần tử (xem Phụ lục 10), _khả năng biểu diễn của DeltaFormer vượt TC⁰ và đạt tới NC¹._

##### Triển khai Thuật toán.

Chúng ta bắt đầu bằng việc phân tích độ phức tạp của thuật toán. Xét các vector 𝒒,𝒌,𝒗,𝒖∈ℝ^d Để tính tích vô hướng, 𝒒 và 𝒌 phải có cùng chiều. Phương trình 39 ngụ ý rằng 𝒗 và 𝒖 cũng chia sẻ cùng chiều. Mặc dù chiều của 𝒌 và 𝒗 có thể khác nhau, sự khác biệt này thường bị giới hạn bởi một hệ số không đổi. Để đơn giản, chúng tôi giả định chúng có cùng chiều., và T là độ dài chuỗi.

* _Giai đoạn huấn luyện._ Đầu tiên, chúng ta tính toán chuỗi {𝒖}_{i=1}^{T}. Quan sát Phương trình 39, độ phức tạp tính toán ở đây được định nghĩa là số phép nhân. cho mỗi 𝒖 là 𝒪​(T​d). Do đó, độ phức tạp tính toán cho toàn bộ chuỗi là 𝒪(T^{2}d). Vì 𝒖_{t} phụ thuộc vào lịch sử {𝒖}_{i=1}^{t-1}, độ phức tạp thời gian tuần tự là 𝒪​(T). Sau khi tính toán xong {𝒖}_{i=1}^{T}, các phép toán trong Phương trình 40 có độ phức tạp tính toán và thời gian lần lượt là 𝒪​(T^{2}​d) và 𝒪​(1). Vì vậy, tổng độ phức tạp tính toán và thời gian trong quá trình huấn luyện là 𝒪​(T^{2}​d) và 𝒪​(T).

* _Giai đoạn suy luận._ Đầu tiên, chúng ta tính 𝒖_{t} và tích hợp nó với 𝒌_{t} để cập nhật 𝑺_{t}. Độ phức tạp tính toán và thời gian tương ứng là 𝒪​(T​d) và 𝒪​(1). Sau đó, 𝒒_{t} được sử dụng để truy xuất đầu ra cuối cùng 𝒐_{t} từ 𝑺_{t}, với độ phức tạp tính toán và thời gian là 𝒪​(d^2) và 𝒪​(1). Do đó, tổng độ phức tạp tính toán và thời gian trong suy luận là 𝒪​(T​d+d^2) và 𝒪​(1).

Theo phân tích trên, độ phức tạp thời gian 𝒪​(T) trong huấn luyện kém hơn đáng kể so với 𝒪​(1) của softmax attention. Vì vậy, cải thiện tính song song trong huấn luyện là rất quan trọng. Để giải quyết vấn đề này, chúng ta có thể chia chuỗi thành các đoạn và tính toán song song hoàn toàn trong mỗi đoạn. Kết quả là độ phức tạp thời gian huấn luyện tổng thể có thể được tối ưu thành 𝒪​(T/C), với CC là kích thước đoạn. Chi tiết triển khai và mã giả có thể xem trong Phụ lục 9.3. Ngoài ra, chúng tôi cung cấp mã tham khảo cho thí nghiệm theo dõi trạng thái DeltaFormer trong Phụ lục 10.4.

#### 2.2.2 Tình huống 3: Học trong ngữ cảnh dài vô hạn

Như Bảng 2 cho thấy, các mô hình bộ nhớ liên hợp khác nhau tương ứng với các mục tiêu tối ưu hóa cập nhật bộ nhớ khác nhau. Lấy softmax attention làm ví dụ, trong quá trình giải mã khi kiểm tra (tức học trong ngữ cảnh - ICL), bộ nhớ tương ứng 𝑺 trải qua các bước cập nhật gradient descent đơn lẻ. Xét trường hợp cực đoan: _khi độ dài ICL tiến tới vô hạn, quá trình tối ưu hóa sẽ diễn ra thế nào?_ Nghiên cứu trường hợp giới hạn này có thể giúp dự đoán sự phát triển của các mô hình cho ngữ cảnh cực dài.

**DeltaNet đơn đầu.** Cụ thể, trước tiên chúng tôi xem xét một mô hình đại diện đơn giản: DeltaNet với attention đơn đầu¹⁴. Mục tiêu tối ưu của DeltaNet là:

$$\mathcal{L}_t(\mathbf{S}_{t-1}) = \frac{1}{2}\|\mathbf{S}_{t-1}\mathbf{k}_t - \mathbf{v}_t\|^2. \quad (44)$$

Hãy thử suy ra bộ nhớ tối ưu $\mathbf{S}^*$ tương ứng với mục tiêu tối ưu này:

$$\mathbf{S}^* = \arg\min_{\mathbf{S}} \mathbb{E}_{\mathbf{x}} \left[\frac{1}{2}\|\mathbf{S}\mathbf{W}_k\mathbf{x} - \mathbf{W}_v\mathbf{x}\|^2\right], \quad (45)$$

trong đó $\mathbf{x}$ biểu thị biến đầu vào, $\mathbf{k} = \mathbf{W}_k\mathbf{x}$, $\mathbf{v} = \mathbf{W}_v\mathbf{x}$, và $\{\mathbf{W}_k, \mathbf{W}_v\}$ là các trọng số mô hình cố định tại thời điểm kiểm tra. Theo Phụ lục F, nghiệm giải tích cho $\mathbf{S}^*$ là:

$$\mathbf{S}^* = (\mathbf{W}_v \mathbb{E}_{\mathbf{x}}[\mathbf{x}\mathbf{x}^{\top}]\mathbf{W}_k^{\top})(\mathbf{W}_k \mathbb{E}_{\mathbf{x}}[\mathbf{x}\mathbf{x}^{\top}]\mathbf{W}_k^{\top})^{-1}. \quad (46)$$

Vì $\mathbf{W}_k$ thường khả nghịch¹⁵ và $\mathbb{E}_{\mathbf{x}}[\mathbf{x}\mathbf{x}^{\top}]$ nhìn chung là xác định dương. Do đó, nghiệm giải tích được đơn giản hóa thành:

$$\mathbf{S}^* = \mathbf{W}_v\mathbf{W}_k^{-1}. \quad (47)$$

Điều này cho thấy rằng, lý tưởng nhất, khi quá trình cập nhật bộ nhớ tiến triển, bộ nhớ $\mathbf{S}$ hội tụ về giá trị cố định $\mathbf{S}^*$. Khi bộ nhớ không còn được cập nhật, có nghĩa là không có thông tin mới nào được ghi vào bộ nhớ, và kết quả nhớ lại chỉ phụ thuộc vào $\mathbf{S}^*$ cố định. Nếu $\mathbf{S}^*$ được xem như một phần của trọng số mô hình, DeltaNet đơn đầu `suy biến thành một mô hình bi-gram`, trong đó đầu ra chỉ được xác định bởi đầu vào hiện tại. Về mặt lý thuyết, **khi độ dài ngữ cảnh tiến tới vô cùng, DeltaNet đơn đầu mất khả năng học trong ngữ cảnh**.

**DeltaNet đa đầu.** Trong trường hợp thiết lập đa đầu, $\mathbf{W}_k$ và $\mathbf{W}_v$ trở thành các ma trận hạng thấp. Phương trình 46 do đó được đơn giản hóa thành:

$$\mathbf{S}^* = \mathbf{W}_v \mathbb{E}_{\mathbf{x}}[\mathbf{x}\mathbf{x}^{\top}] \mathbf{W}_k^{+} \mathbf{W}_k \mathbb{E}_{\mathbf{x}}[\mathbf{x}\mathbf{x}^{\top}]^{-1} \mathbf{W}_k^{+}, \quad (48)$$

trong đó $\mathbf{W}_k^{+}$ biểu thị nghịch đảo giả của $\mathbf{W}_k$. Vì nhìn chung $\mathbf{W}_k^{+}\mathbf{W}_k \neq \mathbf{I}$, số hạng $\mathbb{E}_{\mathbf{x}}[\mathbf{x}\mathbf{x}^{\top}]$ không thể bị loại bỏ. Điều này ngụ ý rằng bộ nhớ tối ưu $\mathbf{S}^*$ phụ thuộc vào phân phối dữ liệu và có thể không cố định. **Tính chất hạng thấp của các ma trận chiếu (ví dụ: $\mathbf{W}_k$) được giới thiệu bởi attention đa đầu có thể làm giảm bớt sự suy biến của khả năng học trong ngữ cảnh của mô hình.**

¹⁴Mục tiêu tối ưu tương ứng với linear attention vanilla là không bị chặn. Khi $t$ tăng, chuẩn của $\mathbf{S}\mathbf{k}$ bùng nổ, gây ra sự sụp đổ mô hình. Điều này ngụ ý rằng linear attention (không có các tối ưu hóa như suy giảm bộ nhớ) không thể thực hiện học trong ngữ cảnh rất dài. Mục tiêu tối ưu cho DeltaNet bị chặn, tránh các vấn đề nói trên, làm cho nó trở thành điểm khởi đầu hợp lý hơn cho phân tích và so sánh.

¹⁵Các ma trận vuông thường có hạng đầy đủ, và do đó khả nghịch.

**Hình 2** Ảnh hưởng của số chiều dưới các cơ chế attention khác nhau. Loss validation với GPT-2 small (12L, 768D). Giảm số lượng đầu có lợi cho Linear Attention nhưng gây hại cho Softmax Attention, có thể do sự khác biệt trong tính chất truy xuất và biểu diễn của chúng.

**Softmax Attention.** Với việc cấu trúc đa đầu có thể ngăn chặn sự suy giảm ICL, liệu điều này có nghĩa không còn rào cản lý thuyết nào trong việc xây dựng các mô hình hỗ trợ độ dài ngữ cảnh vô hạn? Ít nhất đối với softmax attention, điều này không đúng.

Theo Bảng 2, mục tiêu tối ưu của softmax attention là:

$$\mathcal{L}_t(\mathbf{S}_{t-1}) = \frac{1}{t}\left(\frac{1}{2}\|\mathbf{S}_{t-1}\|_F^2 - \langle\mathbf{S}_{t-1}\phi(\mathbf{k}_t), \mathbf{v}_t\rangle\right). \quad (49)$$

Khi $t$ đủ lớn, do sự hiện diện của số hạng $\frac{1}{t}$, ta có $\mathcal{L}_t(\mathbf{S}_{t-1}) \rightarrow 0$, khiến gradient tiến về không và $\mathbf{S}$ dần dần ngừng cập nhật. **Điều này ngụ ý rằng khả năng ICL của softmax attention vẫn sẽ dần dần giảm khi độ dài ngữ cảnh tăng.** Một cách trực quan, khi ngữ cảnh, tức là bộ nhớ KV cache, trở nên quá dài, thông tin được lưu trữ trong ngữ cảnh giống như một "đại dương rộng lớn" với tỷ lệ tín hiệu trên nhiễu thấp, khiến việc nhớ lại chính xác thông tin mục tiêu trở nên khó khăn, do đó làm cho ICL không khả thi.

Nếu chúng ta loại bỏ chuẩn hóa, mặc dù gradient không biến mất khi $t$ tăng, softmax attention không có chuẩn hóa tương ứng với một mục tiêu tối ưu không bị chặn, cuối cùng sẽ dẫn đến sự sụp đổ mô hình.

**Tóm tắt.** Các mô hình bộ nhớ liên kết về mặt lý thuyết có nguy cơ hội tụ dần về một giá trị cố định khi độ dài chuỗi tăng. Cụ thể đối với lớp attention, bộ nhớ có xu hướng dần dần ngừng cập nhật, dẫn đến khả năng ICL bị suy giảm. Do đó, **việc xây dựng các mô hình có khả năng xử lý ngữ cảnh vô hạn có thể là thách thức.** Nhưng cần thừa nhận rằng các kịch bản thực tế phức tạp hơn đáng kể: các cơ chế như attention đa đầu, suy giảm bộ nhớ, và tích chập khóa-giá trị ngắn có thể ngăn chặn hiệu quả việc cập nhật bộ nhớ bị đình trệ. Hơn nữa, các mạng neural lớn thường thể hiện cấu trúc đa lớp, trong đó các dao động trong tối ưu hóa đơn lớp (ví dụ: sự biến thiên được giới thiệu bởi $\mathbb{E}_{\mathbf{x}}[\mathbf{x}\mathbf{x}^{\top}]$) có thể tích lũy và khuếch đại qua các lớp, do đó cản trở bộ nhớ tiếp cận một cách mượt mà nghiệm giải tích lý thuyết.

##  3 Experiments

###  3.1 Multi-head Efficiency Trade-off

Figure 2: Effect of dimension under different attention mechanisms. Validation loss with GPT-2 small (12L, 768D). Reducing head numbers benefits Linear Attention but harms Softmax Attention, likely due to differences in their retrieval and expressive properties.

We conduct experiments on a GPT-2 small Transformer with 12 layers and 768 hidden dimensions to study how attention mechanisms respond to head count versus per-head dimensionality. Our setup follows the open-source BlaGPT implementation 161616https://github.com/erogol/BlaGPT, including dataset and training configuration.

For the linear attention setting, we simply replace the Softmax operation with a linear kernel function and apply per-head RMS normalization to maintain training stability. To ensure fair comparison, we also remove the original normalization term from the Softmax baseline and apply per-head RMS norm instead.

As shown in Figure 2, Linear Attention benefits significantly from reducing the number of heads to 1, thereby increasing head dimensionality, while Softmax Attention degrades under the same condition. This supports our hypothesis that Softmax relies more on its expressiveness through multi-head, while Linear Attention gains more from greater memory capacity. These findings provide preliminary empirical validation for the hypothesis in Sec. 2.1.

###  3.2 Track the Exchange of Elements

Figure 3: Swap task diagram. At the beginning, tokens of different colors are placed at positions 11 to 55, and the tokens of two positions are exchanged at each step. We expect the model to query what the token for each position is at each step. Simply but without loss of generality, we default to outputting the token at the first position to avoid introducing a "query token". This task can also be tokenized into a task with an input vocabulary size of C52=10C_{5}^{2}=10 and an output vocabulary size of 55.

Although our Theorem 1 proves that DeltaFormer we describe in Section 2.2.1 can track the exchange of nn objects, it still needs to be proven through experiments. By using gradient descent, can DeltaFormer learn to track the exchange of nn objects from the data? For this purpose, we conduct an experiment in this section to verify this. The setting is shown in Figure 3, and the default context length is 1616. 171717For simplicity, we omit the temperature coefficient used in the similarity function in this section.

(a) Transformer.  


(b) DeltaFormer. (κ1​(𝒙,𝒚)=⌊𝒙⊤​𝒚⌋\kappa_{1}(\bm{x},\bm{y})=\lfloor\bm{x}^{\top}\bm{y}\rfloor)

(c) DeltaFormer. (κ1​(𝒙,𝒚)=𝒙⊤​𝒚\kappa_{1}(\bm{x},\bm{y})=\bm{x}^{\top}\bm{y})  


(d) DeltaFormer. (κ1​(𝒙,𝒚)=max⁡(𝒙⊤​𝒚,0)\kappa_{1}(\bm{x},\bm{y})=\max(\bm{x}^{\top}\bm{y},0))

(e) DeltaFormer. (κ1​(𝒙,𝒚)=exp⁡(𝒙⊤​𝒚)\kappa_{1}(\bm{x},\bm{y})=\exp(\bm{x}^{\top}\bm{y}))

(f) DeltaFormer. (κ1​(𝒙,𝒚)=s​o​f​t​m​a​x​(𝒙⊤​𝒚)\kappa_{1}(\bm{x},\bm{y})=softmax(\bm{x}^{\top}\bm{y}))

Figure 4: Comparison of Transformer and DeltaFormer using different similarity functions κ1​(⋅)\kappa_{1}(\cdot) for performing swapping tasks. For κ2​(⋅)\kappa_{2}(\cdot), we use the softmax function to maintain consistency with Transformer. Pay attention to the scale of the y-axis. To ensure convergence, ⌊⋅⌋\lfloor\cdot\rfloor means round to two decimal, such as ⌊1.236⌋=1.24\lfloor 1.236\rfloor=1.24.

DeltaFormer Can Track the Exchange of Elements. We compared DeltaFormer and Transformer under different similarity function designs, as shown in Figure 4. Under almost imaginable simple κ1​(⋅)\kappa_{1}(\cdot) designs, DeltaFormer has achieved better results than Transformer models. And the 1-layer DeltaFormer can execute and track the exchange operations of 55 elements. But increasing the number of layers in the transformer did not improve either. We speculate that there may be optimization issues involved, even with DeltaFormer. We will explore this hypothesis in the following sections.

The Kernel κ1\kappa_{1} Used in ut\bm{u}_{t} Is important. Another noteworthy point is that the choice of different similarity functions also has a significant impact on the final effect. According to our construction proof in Theorem 1, the similarity function can effectively track 55 elements. The closer the similarity function is to the constructive method, the better the performance. The normalization term of s​o​f​t​m​a​xsoftmax has a negative impact on the similarity calculation of e​x​pexp. And the κ2\kappa_{2} used in our reading method is not based on the constructed similarity to ensure that it is the same as the standard attention, but instead uses softmax. Even so, a suitable κ1\kappa_{1} can achieve 100% effectiveness. Because our Theorem 1 actually proves that there is a way to read the elements at each position in a certain form of 𝒖\bm{u}. This means that the exchange of elements is implicitly included in the update of 𝒖\bm{u}. Intuitively speaking, if the similarity selection of κ1\kappa_{1} is not appropriate, it will cause more cumulative errors in the update of 𝒖\bm{u}. Mathematically speaking, it actually reflects the perturbation of a inverse matrix is likely to be ill-conditioned. We rewrite the calculations for 𝒖\bm{u} and 𝒐\bm{o} as follows:

| 𝒖\displaystyle\bm{u} | =𝑨1−1​𝒗\displaystyle=\bm{A}_{1}^{-1}\bm{v} |   
---|---|---|---  
| 𝒐\displaystyle\bm{o} | =𝑨2​𝒖,\displaystyle=\bm{A}_{2}\bm{u}, |  | (50)  
  
then we will have:

| ‖(𝑨1+Δ​𝑨)−1​𝑽−𝑨1−1​𝑽‖≈‖𝑨1−1​(Δ​𝑨)​𝑨1−1​𝑽‖≤‖𝑨1−1‖​‖Δ​𝑨‖​‖𝑨1−1‖​‖𝑽‖=‖𝑨1−1‖2​‖Δ​𝑨‖​‖𝑽‖,\displaystyle\|(\bm{A}_{1}+\Delta\bm{A})^{-1}\bm{V}-\bm{A}_{1}^{-1}\bm{V}\|\approx\|\bm{A}_{1}^{-1}(\Delta\bm{A})\bm{A}_{1}^{-1}\bm{V}\|\leq\|\bm{A}_{1}^{-1}\|\|\Delta\bm{A}\|\|\bm{A}_{1}^{-1}\|\|\bm{V}\|=\|\bm{A}_{1}^{-1}\|^{2}\|\Delta\bm{A}\|\|\bm{V}\|, |  | (51)  
---|---|---|---  
  
and

| ‖(𝑨2+Δ​𝑨2)​𝑼−𝑨2​𝑼‖=‖(Δ​𝑨2)​𝑼‖≤‖Δ​𝑨2‖​‖𝑼‖.\displaystyle\|(\bm{A}_{2}+\Delta\bm{A}_{2})\bm{U}-\bm{A}_{2}\bm{U}\|=\|(\Delta\bm{A}_{2})\bm{U}\|\leq\|\Delta\bm{A}_{2}\|\|\bm{U}\|. |  | (52)  
---|---|---|---  
  
The stability of the calculation for 𝒖\bm{u} is weaker than that for 𝒐\bm{o}, so the calculation for 𝒖\bm{u} need to balance stability and expressivity, and we provide a group query attention like approach to balance expressiveness and training stability as shown in the Appendix 10.2.

Stress Testing of Linear and Nonlinear Kernels. We also conducted stress tests on the round and linear function in this Section, with d=128d=128. The setting is similar to Section 3.2, but with experiments where nn is greater than or equal to 128128. We use n∈{128,256,512}n\in\\{128,256,512\\}, and the corresponding training length is {256,512,1024}\\{256,512,1024\\} to ensure as much as possible that most elements participate in the exchange. In addition, to avoid optimization issues, we adopted the almost orthogonal vectors used in our Theorem 1 to set key and value of the model and the model only needs to learn to read information from the state space. The results is shown in Figure 5. We can observe that when dd is fixed, as n≥dn\geq d increases, the performance of the linear kernel is severely degraded. This essentially involves the famous Thompson problem, which is how to place as many orthogonal vectors as possible on the d-dimensional unit sphere. However linear functions cannot have superposition, and nonlinear functions can store a large amount of information through superposition [9].

(a) d=128d=128, n=128n=128.

(b) d=128d=128, n=256n=256.

(c) d=128d=128, n=512n=512.

Figure 5: Comparison of DeltaFormer with κ​(𝒙,𝒚)=𝒙⊤​𝒚\kappa(\bm{x},\bm{y})=\bm{x}^{\top}\bm{y} and κ​(𝒙,𝒚)=⌊𝒙⊤​𝒚⌋\kappa(\bm{x},\bm{y})=\lfloor\bm{x}^{\top}\bm{y}\rfloor.

Curriculum Learning Is Important. As shown in the figure, we initially trained at a length of 256256, and the convergence speed of the model was very slow. So we decided to gradually lengthen the window from 3232, that is, gradually increase the difficulty. We find that on the basis of such curriculum learning, the model can achieve better performance with fewer computation and samples. A similar phenomenon has also been observed in using Transformers in-context [12].

(a) DeltaFormer with RoPE.

(b) DeltaFormer with NoPE.

Figure 6: Comparison of DeltaFormer using different learning strategy and position embedding. Each use κ1​(𝒙,𝒚)=⌊𝒙⊤​𝒚⌋\kappa_{1}(\bm{x},\bm{y})=\lfloor\bm{x}^{\top}\bm{y}\rfloor. “32 to 256” means that the initial training length is 32, which means the number of swaps is 32. When the accuracy reaches 0.99, the training length will be doubled until it reaches 256. Each color gradient in the image represents a doubling of the training length. And “256” means that the model is trained on a training length of 256 from the beginning. The y-axis reflects the accuracy at the current training length.

The Role of Rotary Embeddings [39]. Because our proof of Theorem 1 does not require positional embeddings. Therefore, another interesting experiment is that we removed the default rotary position embeddings which is widely used in modern Transformer. We find that after removing the positional embedding, the convergence of the model slowed down, and even when trained directly at a length of 256256, the model get a random score. On the other hand, after removing RoPE, “32 to 256” can achieve 100% accuracy. And during the extension process, the performance degradation of NoPE at jump points decreases, indicating that NoPE has better length generalization. There are also some studies [18] that have made similar findings regarding the length generalization of position embedding. Therefore, we speculate that RoPE may have damaged the expression of model and extrapolation ability, but it is more conducive to optimization.

###  3.3 Reachability of Directed Acyclic Graphs

Furthermore, we have a simple graph connectivity task, which involves determining reachability on a directed acyclic graph. For simplicity, we only consider whether other nodes can be reached by the first node in a certain topology ranking. And each node encodes at most its neighboring node information at the beginning. Due to the final output being true and false, in order to avoid class imbalance, we construct the input data by dividing nn points into 22 classes on average, with one tree for each class. So we only judge the reachability of other nodes starting from a certain root node. Then we only encode the parent node information for each node.

(a) Transformer.

(b) DeltaFormer.

Figure 7: Comparison of Transformer and DeltaFormer using different similarity functions κ1​(⋅)\kappa_{1}(\cdot) for performing the reachability of directed acyclic graphs tasks. For κ2​(⋅)\kappa_{2}(\cdot), we use the softmax function to maintain consistency with the standard Transformer.

DeltaFormer Performs Better Than Transformer. We conducted experiments on 3232 nodes, as shown in Figure 7. A multi-layer transformer model is also difficult to achieve 100% accuracy, but a single-layer DeltaFormer can do very well. In theory, a transformer requires 𝒪​(log⁡n)\mathcal{O}(\log n) layers to perform connectivity checks with nn nodes [32]. However, based on Figure 7(a), we speculate that the optimization problem also constrains the effectiveness of the Transformer in this task.

The Power of Matrix Inverse. As shown in Eq. 97, the calculation of 𝒖\bm{u} can be rewrite by matrix inverse. If the adjacency matrix 𝑨\bm{A} is known, then to determine whether nodes ii and jj are connected, simply calculate 𝑨,𝑨2,𝑨3,…,𝑨n\bm{A},\bm{A}^{2},\bm{A}^{3},...,\bm{A}^{n} in sequence, and then observe whether the (i,j)(i,j) elements of this matrix are greater than 0. And (𝑰−𝑨)−1(\bm{I}-\bm{A})^{-1} can be seen as an approximation of 𝑰+𝑨+⋯+𝑨n\bm{I}+\bm{A}+\dots+\bm{A}^{n}. Therefore, the operation of matrix inversion greatly improves the expressivity of the model. We can expect a model with matrix inverse to achieve better performance in graph related tasks. Fagnou et al. [10] also use matrix inversion for efficient entity tracking. Essentially, the operation of matrix inversion, which goes far beyond T​C0TC^{0}, enhances the expressiveness of Transformer.

##  4 Conclusion

This exploration, viewing Transformers through the prism of associative memory, represents but a modest step in our ongoing quest to unravel the full potential and intricate workings of these powerful architectures. While we believe this perspective offers valuable clarity and a unifying framework for understanding phenomena like memory capacity and update dynamics, it simultaneously opens the door to a landscape teeming with further questions and avenues for deeper investigation. We find ourselves particularly intrigued by, and eager to further explore, the following fundamental inquiries:

The Architectural Endgame: Is there an ultimate, optimal design for model architectures? Is associative memory, as we’ve discussed it, the most effective paradigm for achieving artificial intelligence? If this framework is indeed central, are our current Transformer structures—with their distinct attention and feed-forward layers—approaching a fundamental optimum, or are there radically different, yet more potent, instantiations of associative memory waiting to be discovered?

Bridging Contextual and Persistent Memory: Why do current Transformers necessitate a separation between dynamic Context Memory (Attention) and static Persistent Memory (FFNs)? Human cognition features sophisticated mechanisms, like the role of the hippocampus, for filtering, consolidating, and transferring salient contextual experiences into long-term, persistent knowledge. When might our models acquire analogous capabilities? Such an advancement could allow them to learn continuously and adaptively during test time, potentially mitigating the challenges posed by infinitely long contexts. A critical sub-question here is: if such a consolidation mechanism were to be developed, what would be the intrinsic reward signal or loss function to guide its learning and refinement?

Parallelism, Hardware, and the Next Leap in Intelligence: The inherent parallel complexity of existing Transformer models (largely within T​C0TC^{0}) imposes limits on the types of problems they can efficiently learn and solve. This limitation is intrinsically linked to the capabilities of our current parallel hardware. Is there significant merit in pursuing models with higher parallel complexity classes (e.g., T​C1TC^{1} or even P-complete tasks)? Does the advent of next-generation intelligence hinge upon a paradigm shift in hardware—perhaps the maturation of technologies like quantum computing, which theoretically promise efficient solutions to problems currently intractable within T​CkTC^{k}?

These questions underscore the vast and fertile ground that still lies before us. The journey to understand and replicate intelligence is far from over. Each answer we find tends to illuminate a dozen new questions, each more profound than the last. It is our hope that by posing these challenges and sharing our current understanding, we can stimulate further discussion, collaboration, and innovation within the research community, collectively pushing the boundaries of what artificial intelligence can achieve. The path ahead is complex, but the pursuit of these fundamental questions promises a future rich with discovery.
